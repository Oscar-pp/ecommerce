<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Productos - MyShop</title>
</head>
<body>
    <h1>Lista de Productos</h1>
    <ul>
        <% productos.forEach(producto => { %>
            <li>
                <strong><%= producto.nombre %></strong> -
                <%= producto.descripcion %> -
                $<%= producto.precio %>
            </li>
        <% }); %>
    </ul>
</body>
</html>